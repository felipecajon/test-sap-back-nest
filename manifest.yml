applications:
  - name: test-sap-back-nest
    path: ./
    buildpacks:
      - nodejs_buildpack
    memory: 1024M
    command: npm run build && npm run start:prod
    random-route: true
    services:
      - Destinations
      - my-xsuaa